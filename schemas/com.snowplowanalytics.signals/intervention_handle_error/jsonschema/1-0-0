{
	"$schema": "http://iglucentral.com/schemas/com.snowplowanalytics.self-desc/schema/jsonschema/1-0-0#",
	"description": "Schema for event when a handler function unsuccessfully processes a received intervention.",
	"self": {
		"vendor": "com.snowplowanalytics.signals",
		"name": "intervention_handle_error",
		"format": "jsonschema",
		"version": "1-0-0"
	},
	"type": "object",
	"properties": {
		"handler": {
			"description": "The name of the handler that processed the intervention unsuccessfully.",
			"type": "string",
			"maxLength": 255
		},
		"error": {
			"description": "Error text describing why the handler was unable to handle the received intervention.",
			"type": ["string", "null"],
			"maxLength": 1024
		}
	},
	"required": ["handler"],
	"additionalProperties": false
}
