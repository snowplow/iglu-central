{
		"$schema": "http://iglucentral.com/schemas/com.snowplowanalytics.self-desc/schema/jsonschema/1-0-0#",
		"description": "Schema for a deeplink opened event.",
		"self": {
			"vendor": "com.snowplowanalytics.mobile",
			"name": "deeplink_opened",
			"format": "jsonschema",
			"version": "1-0-0"
		},
		"type": "object",
		"properties": {
      "provider": {
        "type": "string",
        "maxLength": 100,
        "description": "The name of the provider associated with the deeplink service e.g Branch Metrics."
      },
      "url": {
        "type": "string",
        "maxLength": 1000,
        "description": "The URL of the deeplink that was opened."
      },
			"type": {
				"type": ["string", "null"],
				"maxLength": 1000,
				"description": "The type of deeplink that was opened e.g Re-engagement campaign."
			}
		},
		"minProperties": 1,
		"required": ["provider", "url"],
		"additionalProperties": false
}
